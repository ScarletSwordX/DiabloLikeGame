namespace Gameplay.Data
{
    /// <summary>
    /// 状态在角色上的作用方式（由 StatusDefinition 指定）。
    /// </summary>
    public enum StatusKind
    {
        DamageReduction,
        MoveSpeedMultiplier,
        Dizzy,
        DamageOverTime
    }
}
